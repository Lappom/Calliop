[package]
name = "calliop-prompt"
version = "0.1.1"
edition = "2021"
description = "Shared LLM cleanup prompts for Calliop"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
